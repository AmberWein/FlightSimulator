<Page x:Class="FlightSimulator.Views.FlightSimulatorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FlightSimulator.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FlightSimulatorView">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:DashboardView x:Name="dash" Grid.Row="0" />
        <local:MediaPlayerView x:Name="media" Grid.Row="2" Margin="0,1,0,-2.8"/>
        <local:GearControlView x:Name="gear" Grid.Row="1" />
        <local:DetectorView x:Name="detector" Grid.Row="3"/>
    </Grid>
</Page>
