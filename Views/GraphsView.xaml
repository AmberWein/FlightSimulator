<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightSimulator.Views"
                 xmlns:oxy="http://oxyplot.org/wpf"

             xmlns:Syncfusion="http://schemas.syncfusion.com/wpf" x:Class="FlightSimulator.Views.GraphsView"
    mc:Ignorable="d" 
    
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <oxy:Plot x:Name="model"  Title="{Binding Title}" FontFamily="Bosch Sans Medium" Foreground="#FF0C6596" FontSize="19" Canvas.Left="298" Canvas.Top="32" Background="AliceBlue" Margin="0,0,10,0" BindingGroup="{Binding ElementName=ls, Mode=OneWay}">
            <oxy:Plot.Series>
                <oxy:ScatterSeries x:Name="ls" Background="White" ItemsSource="{Binding VM_Data}" Color="ForestGreen" MarkerType="None" MarkerSize="5" MarkerFill="Black">

                </oxy:ScatterSeries>
            </oxy:Plot.Series>
        </oxy:Plot>



    </Grid>
</UserControl>
